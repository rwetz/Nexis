import type { CapabilityDefinition } from "@/workbench/capability";

/** Python environment discovery is shared by status UI and ML Lab. */
export const pythonCapability: CapabilityDefinition = {
  id: "python.environments",
  panels: [],
};
