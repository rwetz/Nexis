coverage

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

# Rust / Tauri
src-tauri/target

# E2E test artifacts
e2e/logs/
e2e/.wdio-*
e2e/.drivers/
wdio-*.json

# Leftover source archives

# Obsidian personal config (docs/vault is the shared vault; .obsidian is per-user)
docs/vault/.obsidian/
.claude/settings.local.json

# Per-assistant duplicates of CLAUDE.md (AGENTS.md is a Codex-facing copy).
# CLAUDE.md is the one tracked copy of the invariants -- a second one in git
# is a second thing to keep in sync, and the pitfall numbering drifts first.
AGENTS.md
