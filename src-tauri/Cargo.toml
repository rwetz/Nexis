[package]
name = "nexis-atlas"
version = "0.3.0"
description = "Every git repo on your machine, as a list and as a map — one scan, two views."
authors = ["Ryan Wetzstein"]
edition = "2021"

[lib]
name = "nexis_atlas_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-os = "2"
tauri-plugin-opener = "2"
tauri-plugin-window-state = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
# Read-only local scanning — no network transports needed, so default
# features (ssh/https) are disabled to keep the build lean.
git2 = { version = "0.20", default-features = false }
rayon = "1.10"
dirs = "6"

[profile.release]
codegen-units = 1
lto = true
strip = true
